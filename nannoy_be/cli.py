import sys 
#This module allow access to variables used/maintained by the intepreter , include to run with the runtime env.
#& sys.argv is one variable used to handle command line argument 

print(sys.argv[0])  #output :the name of the script itself 
print(str(sys.argv))